import styled from 'styled-components';

export const StyledLogoWrapper = styled.div`
  display: flex;
  align-items: center;
  width: fit-content;
  grid-column-gap: 16px;

  svg,
  img {
    height: inherit;
  }
`;
